$(function() {
    /*var url_atual = window.location.pathname;

    if (url_atual == '/switches_backups/') {

    }
    if (url_atual == '/') {
        console.log('oi');
    }
    Quando o botão do switch é clicado, os demais são adicionados via js
    $('#btn_switches').click(function() {
        const botao = $(this).attr('id');
        $('#btn_aps').hide();
        // var token = document.getElementsByName('csrfmiddlewaretoken')[0].value;

    });*/


});